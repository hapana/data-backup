version: "3"
services:
  portainer:
    image: portainer/portainer-ce
    user: ${CURRENT_UID}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
    ports:
      - 8000:8000
      - 9000:9000
